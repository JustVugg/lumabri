# A unified diff uses a single space for an empty context line. Keep the
# whitespace check for C/Python sources without flagging that required syntax.
engine_patches/*-p2p.diff whitespace=-blank-at-eol
